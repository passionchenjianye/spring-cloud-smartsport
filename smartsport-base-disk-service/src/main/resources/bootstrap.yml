eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8081/eureka/
server:
  port: 8082
spring:
  cloud:
    config:
      label: master
      uri: http://localhost:8087/
      profile: test
      name: config-client
  application:
    name: base-disk-service
management:
  security:
    enabled: false